import React from 'react'

function Comment(props) {
    return (
        <div>
          {/* { if(props.comments){ props.comments.map((comment)=><p>{comment.body}</p>)} } */}
        </div>
    )
}

export default Comment
